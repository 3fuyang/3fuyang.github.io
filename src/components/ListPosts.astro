---
export interface Props {
  posts: {
    slug: string
    title: string
    date: Date
  }[]
}

const { posts } = Astro.props
---

<section class="ma max-w-xl flex flex-col gap-6">
  {
    posts.map(({ slug, title, date }) => {
      const dateTime = new Date(date).toLocaleDateString()
      return (
        <article>
          <a
            href={slug}
            class:list={[
              'block box-border rounded-sm op75 dark:op80 ml-4 p-1',
              'flex flex-col',
              'transition-all',
            ]}
          >
            <h2 title={title} class="truncate text-xl font-medium focus:op100 hover:op100">{title}</h2>
            <time class="text-sm" datetime={dateTime}>{dateTime}</time>
          </a>
        </article>
      )
    })
  }
</section>
