---
import { Image } from 'astro:assets'

interface Props {
  src: ImageMetadata
  alt: string
}

const { src, alt } = Astro.props
---

<Image
  class="max-w-28 h-a mx-a my-0 lg:max-w-24 rounded b b-gray-6"
  {...{ src, alt }}
>
